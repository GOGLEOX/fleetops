import { EmptyStateCard } from '../components/content/EmptyStateCard'

export function FinancePage() {
  return (
    <EmptyStateCard
      section="Finance"
      summary="Operating ledger"
      detail="Finance will capture practical revenue and cost records for single-player operations without drifting into fake enterprise accounting."
    />
  )
}
