import { EmptyStateCard } from '../components/content/EmptyStateCard'

export function ReportsPage() {
  return (
    <EmptyStateCard
      section="Reports"
      summary="Operational reporting"
      detail="Reports will summarize local trips, fleet utilization, service activity, and finances after those record systems are implemented."
    />
  )
}
